# Environment variable management
python-dotenv==1.0.1

# LangChain and its community dependencies
langchain==0.2.2
langchain-community==0.2.3
langchain-google-genai==1.0.10

# Chroma for vector storage, explicitly using DuckDB instead of SQLite
chromadb==0.5.0
duckdb==0.7.1  # DuckDB dependency to replace SQLite

# OpenAI and Google APIs for embeddings
openai==1.31.1
google-generativeai>=0.7.0,<0.8.0

# Text processing and embeddings
tiktoken==0.7.0
PyPDF2==3.0.1  # For reading PDFs

# Streamlit for the web app interface
streamlit==1.25.0
